{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environment": { "value": "${AZURE_ENV_NAME=dev}" },
    "location": { "value": "${AZURE_LOCATION=eastus2}" },
    "projectName": { "value": "arqcdr" },
    "containerImage": { "value": "${AZURE_CONTAINER_IMAGE=arqcdr:latest}" },
    "azureTenantId": { "value": "${AZURE_TENANT_ID=}" },
    "azureClientId": { "value": "${AZURE_CLIENT_ID=}" },
    "azureClientSecret": { "value": "${AZURE_CLIENT_SECRET=}" },
    "openAiModelName": { "value": "${AZURE_OPENAI_MODEL=gpt-4o-mini}" },
    "openAiModelVersion": { "value": "${AZURE_OPENAI_MODEL_VERSION=2024-08-06}" },
    "openAiSkuName": { "value": "${AZURE_OPENAI_SKU=Standard}" },
    "openAiCapacity": { "value": 1 }
  }
}
