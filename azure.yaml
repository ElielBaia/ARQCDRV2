name: arqcdr
metadata:
  template: arqcdr-azure@0.1.0

infra:
  path: infra
  module: main

services:
  api:
    project: .
    language: ts
    docker:
      path: ./Dockerfile
      remoteBuild: true
    host: containerapp
